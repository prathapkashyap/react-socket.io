import React from 'react';

export default  function About(){

    return(
        <div className="card container">
            <h3>A simple messaging app where users can join different rooms and chat with Each other...</h3>
        </div>
    )
}